def min_max_scaler(data: pd.DataFrame, column_names: List[str]) -> pd.DataFrame:
    """Normalize the given columns using min-max scaling"""
    pass

def z_score_scaler(data: pd.DataFrame, column_names: List[str]) -> pd.DataFrame:
    """Normalize the given columns using z-score scaling"""
    pass
